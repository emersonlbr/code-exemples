// // [condition] ? [answer if true] : [answer if false]
// let num = 10;
// // console.log(num == 10 ? 'sim' : 'nao');
// let a = num == 10 ? 'sim' : 'nao';
// console.log(a);


let number = 10;
if (true) {
    // do something
} else if (true) {
    // do something else if the first is false
} else {
    // do something if all are false
}

switch (expression) {
    case true:
        // do something;
        break;
    case true: 
        // do something;
        break;
    default: // none above is true
        // default answer
        break;
}